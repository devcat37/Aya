// Flutter imports:
import 'package:flutter/material.dart';

const whiteColor = Color(0xFFFFFFFF);
const blackColor = Color(0xFF000000);
const lightSurfaceColor = Color(0xFFDADADA);
const lightGreyColor = Color(0xFFDADADA);

const defaultOptionColor = Color(0xFFAAAAAA);
const chosenOptionColor = Color(0xFF487339);

const greyTextColor = Color(0xFF697180);

const mainGreenColor = Color(0xFF8FD875);
const greenButtonGradient = [mainGreenColor, Color(0xFF71B857)];

// Цвета для иконок.
const orangeImageGradient = [Colors.orange, Colors.deepOrange];
const blueImageGradient = [Colors.lightBlueAccent, Colors.blue];
const greyImageGradient = [Color(0xFFCFD2D5), Color(0xFF9B9FA6)];
const greenImageGradient = [mainGreenColor, Color(0xFF889683)];
const purpleImageGradient = [Colors.purpleAccent, Color(0xFF8e44ad)];
const bloodyImageGradient = [Color(0xFFFF512F), Color(0xFFDD2476)];
const mangoImageGradient = [Color(0xFFEDDE5D), Color(0xFFF09819)];

const imageColors = [
  orangeImageGradient,
  blueImageGradient,
  greyImageGradient,
  greenImageGradient,
  purpleImageGradient,
  bloodyImageGradient,
  mangoImageGradient,
];

// Paddings.
const sidePadding = sidePadding24;
const sidePadding2 = 2.0;
const sidePadding4 = 4.0;
const sidePadding5 = 5.0;
const sidePadding6 = 6.0;
const sidePadding8 = 8.0;
const sidePadding10 = 10.0;
const sidePadding12 = 12.0;
const sidePadding16 = 16.0;
const sidePadding18 = 18.0;
const sidePadding24 = 24.0;
const sidePadding28 = 28.0;
const sidePadding32 = 32.0;
const sidePadding40 = 40.0;
const sidePadding48 = 48.0;
const sidePadding64 = 64.0;

// Border radiuses.
final borderRadius1 = BorderRadius.circular(1.0);
final borderRadius2 = BorderRadius.circular(2.0);
final borderRadius8 = BorderRadius.circular(8.0);
final borderRadius12 = BorderRadius.circular(12.0);
final borderRadius16 = BorderRadius.circular(16.0);
final borderRadius32 = BorderRadius.circular(32.0);

// Radiuses.
const radiusCircular6 = Radius.circular(6.0);
const radiusCircular12 = Radius.circular(12.0);
const radiusCircular32 = Radius.circular(32.0);

// Durations.

/// Стандартная продолжительность анимации. Равно 250 мс.
const defaultAnimationDuration = Duration(milliseconds: 250);

/// 500 мс.
const halfASecond = Duration(milliseconds: 500);

/// 1 секунда.
const aSecond = Duration(seconds: 1);
